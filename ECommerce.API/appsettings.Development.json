{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AppSettings": {
    "Issuer": "${ISSUER}",
    "Audcience": "${AUDIENCE}",
    "Secret": "${APP_SECRET}"
  },
  "ConnectionStrings": {
    "PostgreSqlConnection": "${POSTGRES_CONNECTION_STRING}"
  }
}
